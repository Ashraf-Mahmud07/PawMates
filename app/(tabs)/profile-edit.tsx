// This file has been intentionally left without a default export to avoid creating a
// standalone route in the app router. Profile editing is handled inline inside the
// main profile page (`app/(tabs)/profile.tsx`).

export const _profileEditDisabled = true;
